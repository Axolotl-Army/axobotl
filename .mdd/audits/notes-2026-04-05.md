# MDD Audit Notes -- 2026-04-05

## Features Audited

### [01] Leveling System
**Files read:** src/shared/models/UserLevel.ts, src/shared/models/Guild.ts, src/shared/models/LevelRole.ts, src/shared/models/index.ts, src/bot/utils/levelUtils.ts, src/bot/events/messageCreate.ts, src/bot/commands/rank.ts, src/bot/commands/leaderboard.ts, src/bot/commands/xp.ts, src/bot/commands/levelconfig.ts, src/bot/index.ts
**Findings:**
- OK Model matches doc: UserLevel with composite PK (guildId, userId), xp, level, lastXpAt
- OK Index present: (guildId, xp) for leaderboard queries
- OK Leveling formula matches doc: 50 * L^1.7385
- OK XP clamped to 0 and INTEGER max (2,147,483,647)
- OK Cooldown is in-memory Map, configurable per plugin config
- OK Level-up announces respect MAX_LEVEL_UP_ANNOUNCEMENTS (5) cap
- OK Business rule: leaderboard only shows users with xp > 0
- OK Business rule: /xp remove does not trigger level-up notifications (sub !== 'remove' check at line 116 of xp.ts)
- MEDIUM xp.ts line 117-124: `/xp add` reads level-up template from `Guild.levelUpMessage` directly instead of plugin config -- inconsistent with messageCreate.ts which reads from plugin config. If a guild sets the template via the dashboard (which saves to plugin config), `/xp add` level-up messages will use the old template.
- LOW leaderboard.ts: The `buildLeaderboardLines` function fetches members individually in a loop (line 52) -- could use guild.members.fetch({ user: [...ids] }) for batch fetching to reduce API calls.
- OK All commands use Components V2 with proper flag
**Test coverage:** 1 test file documented (tests/unit/leveling.test.ts)
**Doc accuracy:** Doc references `src/bot/index.ts (modified -- registers leveling plugin with commands)` which is correct. Dependencies section lists 07 and 08 which is correct.

### [02] Dashboard Next.js Migration
**Files read:** src/dashboard/next.config.ts, src/dashboard/lib/auth.ts, src/dashboard/lib/db.ts, src/dashboard/app/api/v1/stats/route.ts, src/dashboard/app/api/v1/logs/route.ts, src/dashboard/app/api/v1/guilds/[id]/route.ts, src/dashboard/app/api/v1/guilds/[id]/channels/route.ts, src/dashboard/app/api/v1/guilds/[id]/roles/route.ts, src/dashboard/app/api/v1/guilds/[id]/plugins/route.ts, src/dashboard/app/api/v1/guilds/[id]/plugins/[pluginId]/route.ts, src/dashboard/app/api/v1/guilds/[id]/plugins/leveling/roles/route.ts, src/dashboard/app/(admin)/dashboard/settings/page.tsx, src/dashboard/app/(admin)/dashboard/plugins/page.tsx, src/dashboard/app/(admin)/dashboard/plugins/leveling/page.tsx, src/dashboard/middleware.ts
**Findings:**
- OK Next.js standalone output with proper external packages
- OK Lazy db imports (lib/db.ts) prevent build-time throws
- OK All API routes use `export const dynamic = 'force-dynamic'`
- OK requireOwner() checks session + BOT_OWNER_ID for all API routes
- OK auth.ts: JWT sessions, Discord provider, secure cookie configuration
- OK Settings page: language, embed colour, command toggles all functional
- OK Plugins page: grid layout with enable/disable toggle
- OK Leveling config page: notifications, XP settings, role rewards cards
- OK channels/roles API routes: fetch from Discord API with bot token, filter appropriately
- OK Stats/logs API routes: proper pagination with query params
- LOW settings/page.tsx duplicates leveling formula (getXpForLevel, getLevelFromXp) for embed preview. Shared utility would reduce drift risk, but low severity since the preview is cosmetic.
- LOW No /api/v1/commands or /api/v1/guilds routes documented in feature 02 source_files list but they exist in the codebase. Doc source_files list needs updating.
**Test coverage:** 0 test files documented in doc, but 5 actual test files exist (guilds.test.ts, logs.test.ts, commands.test.ts, stats.test.ts, auth.test.ts)
**Doc accuracy:** Source file list is outdated -- missing channels, roles, plugins API routes (added by later features). The doc does note "Additional Pages" section listing later additions.

### [03] Security Headers, CSRF & Rate Limiting
**Files read:** src/dashboard/next.config.ts, src/dashboard/middleware.ts, src/dashboard/lib/auth.ts
**Findings:**
- OK Security headers in next.config.ts match doc: X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy, X-DNS-Prefetch-Control, Permissions-Policy
- OK CSRF: origin-based validation for non-safe methods, NextAuth routes excluded
- OK Rate limiting: auth 10/min, api 60/min, sliding window with periodic cleanup
- OK Secure cookies: environment-aware (__Secure- prefix in production)
- OK Rate limit returns 429 with Retry-After, X-RateLimit-Limit, X-RateLimit-Remaining headers
- LOW Rate limit cleanup only runs during request handling (line 30). If no requests arrive for a long time, memory won't be freed. Acceptable for typical usage.
**Test coverage:** 3 test files documented, all exist in codebase
**Doc accuracy:** Good -- matches implementation

### [04] SmartTables
**Files read:** src/dashboard/components/table/DataTable.tsx, src/dashboard/components/table/TablePagination.tsx (existence confirmed)
**Findings:**
- OK DataTable and TablePagination components exist
- OK Used by dashboard pages (commands, logs, overview)
- No code-level issues found
**Test coverage:** 1 test file documented (DataTable.test.tsx -- does NOT exist in tests/)
**Doc accuracy:** test_files lists tests/unit/dashboard/components/DataTable.test.tsx but this file does not exist. Doc is inaccurate.

### [05] Discord Components V2
**Files read:** src/bot/commands/leaderboard.ts, src/bot/commands/help.ts, src/bot/commands/rank.ts, src/bot/commands/ping.ts, src/bot/commands/xp.ts, src/bot/commands/levelconfig.ts, src/bot/events/interactionCreate.ts, src/bot/utils/componentBuilders.ts
**Findings:**
- OK All commands use Components V2 (MessageFlags.IsComponentsV2)
- OK componentBuilders.ts provides shared utilities: createContainer, createGuildContainer, createTitle, createText, createSeparator, createPageFooter, disableAllButtons, createPaginationRow
- OK Leaderboard uses collector pattern with 300s timeout
- OK Collector end removes buttons entirely (feature 06 change) -- sends container-only message
- OK Help command only shows non-admin base commands + enabled plugin commands (feature 06 change)
- OK Rank command uses SectionBuilder with ThumbnailAccessory for avatar
- MEDIUM interactionCreate.ts does not handle button interactions (isButton()). Doc says "interactionCreate event handler is extended to route isButton() interactions." But the current code only handles isChatInputCommand(). Leaderboard pagination works because the collector is set up on the reply message directly (not through interactionCreate), so this is not a bug -- but the doc is misleading.
- LOW info.ts command exists in codebase but is not documented in feature 05 (it was added later). Not a code bug.
**Test coverage:** 2 test files documented (leaderboard-v2, component-builders)
**Doc accuracy:** Doc claims interactionCreate handles isButton() routing but it doesn't -- collector handles it directly on the message. Doc should be updated.

### [06] Guild Configuration
**Files read:** src/shared/models/Guild.ts, src/bot/commands/leaderboard.ts, src/bot/commands/help.ts, src/bot/events/messageCreate.ts, src/dashboard/app/(admin)/dashboard/settings/page.tsx (deferred), src/dashboard/app/api/v1/guilds/[id]/route.ts (deferred)
**Findings:**
- OK Guild model has levelUpChannelId column
- OK leaderboard collector end: removes buttons entirely (container only)
- OK help command: hides admin commands, only shows base + enabled plugin commands
- OK messageCreate: reads levelUpChannelId from plugin config, falls back to message channel
- MEDIUM Guild model still has levelUpMessage and levelUpChannelId columns (lines 19-20, 56-67). Doc 07 says "columns remain on the Guild model temporarily but are no longer read by the bot or dashboard." However, xp.ts line 117-118 still reads Guild.levelUpMessage directly. This is the same finding as 01-MEDIUM.
**Test coverage:** 3 test files documented
**Doc accuracy:** Good

### [07] Plugin System
**Files read:** src/shared/models/GuildPlugin.ts, src/shared/models/LevelRole.ts, src/shared/models/index.ts, src/bot/plugins/registry.ts, src/bot/plugins/pluginCache.ts, src/bot/plugins/commandSync.ts, src/bot/plugins/index.ts, src/bot/index.ts, src/bot/events/ready.ts, src/bot/events/interactionCreate.ts, src/bot/events/messageCreate.ts, src/bot/events/guildCreate.ts
**Findings:**
- OK GuildPlugin model matches doc: composite PK (guildId, pluginId), enabled, config JSONB
- OK LevelRole model matches doc: composite PK (guildId, level), roleId, cumulative
- OK Plugin registry supports registerPlugin, getPlugin, getAllPlugins, getPluginForCommand
- OK Plugin cache: 60s TTL, detects changes, returns changed guild IDs for command re-sync
- OK Command sync: uses REST PUT to atomically replace guild commands
- OK guildCreate: initializes plugins as disabled with default config
- OK ready.ts: migrates existing Guild.levelUpMessage/levelUpChannelId to GuildPlugin config
- OK interactionCreate: checks plugin enabled state before executing plugin commands
- OK messageCreate: checks plugin enabled state before awarding XP
- LOW pluginCache.ts line 13: `disabledCommandsCache` stores stringified sorted array -- fragile. If the array contains unexpected types it could fail silently. Minor concern.
**Test coverage:** 3 test files documented
**Doc accuracy:** Good

### [08] Cumulative Role Rewards
**Files read:** src/shared/models/LevelRole.ts, src/bot/events/messageCreate.ts, src/bot/utils/levelUtils.ts
**Findings:**
- OK LevelRole has cumulative boolean, default false
- OK computeRoleRewardActions: correctly separates cumulative/non-cumulative logic
- OK Non-cumulative: highest earned keeps, all below removed
- OK Cumulative: never added to toRemove list
- OK awardRoleRewards in messageCreate: fetches member, applies toAdd/toRemove with error handling
- OK PUT roles endpoint has retroactive cleanup: batched Discord API calls with delay, fire-and-forget. Verified in route.ts.
**Test coverage:** 1 test file documented
**Doc accuracy:** Good

### [09] Default Embed Colour
**Files read:** src/shared/models/Guild.ts, src/bot/utils/embedUtils.ts, src/bot/utils/componentBuilders.ts
**Findings:**
- OK Guild model has embedColor: STRING(7), nullable, default null
- OK embedUtils.ts: getEmbedColor reads guild.embedColor, falls back to DEFAULT_EMBED_COLOR (0x5865F2)
- OK componentBuilders.ts: createGuildContainer uses getEmbedColor for per-guild accent colour
- OK All commands use createGuildContainer (not hardcoded colour)
**Test coverage:** 1 test file documented
**Doc accuracy:** Good
