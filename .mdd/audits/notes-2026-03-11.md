# MDD Audit Notes — 2026-03-11

## Scope: Full project audit (all features)

---

### [01] Leveling System
**Files read:** UserLevel.ts, Guild.ts, models/index.ts, levelUtils.ts, messageCreate.ts, rank.ts, leaderboard.ts, xp.ts, levelconfig.ts, bot/index.ts
**Findings:**
- No code issues found. Implementation matches MDD documentation accurately.
- All business rules correctly implemented (cooldown, XP clamping, level-up cap at 5 announcements, etc.).
- Bot intents are correct: `GuildMessages` is sufficient since the handler doesn't read message.content.
**Test coverage:** 41 unit tests / 6 slash commands + 1 event handler + 1 utility module
**Doc accuracy:** Documentation matches implementation. `known_issues` correctly states all prior audit findings resolved.

---

### [02-nextjs] Dashboard Next.js Migration
**Files read:** layout.tsx (root), layout.tsx (admin), layout.tsx (auth), page.tsx (overview, commands, logs, login), route.ts (stats, guilds, logs, commands, health, nextauth), auth.ts, db.ts, next.config.ts, useLayoutContext.tsx, data.ts, ProfileDropdown.tsx, SettingsDrawer.tsx, not-found.tsx, error.tsx, helpers/index.ts, e2e/dashboard.spec.ts
**Findings:**
- HIGH: No authorization check against BOT_OWNER_ID. Any Discord user who completes OAuth can access the dashboard. The `BOT_OWNER_ID` env var is defined in `.env.example` but never referenced in dashboard code. All dashboard pages and API routes should be restricted to the bot owner.
- MEDIUM (Bug): `useLayoutContext.tsx:92` — `reset()` function references `document.getElementById('theme-style')` but the actual element ID in `app/layout.tsx:33` is `app-theme`. The Factory Reset button will not clear the theme stylesheet link.
- MEDIUM: MDD doc claims rate limiting ("API routes limited to 60 req/min; page routes limited to 100 req/15min") but no rate limiting middleware exists anywhere in the dashboard.
- MEDIUM: MDD doc mentions `requireOwner` middleware but it does not exist.
- MEDIUM: `SettingsDrawer.tsx` is 257 lines — approaching the 300-line quality gate.
- LOW: `/api/v1/commands` endpoint not listed in MDD doc routes section.
- LOW: `/api/v1/logs` pagination behavior not documented in MDD doc.
- LOW: `useLayoutContext.tsx:158` — `useEffect` depends on `settings` object but `settings` is replaced on every render by `useLocalStorage`, causing the effect to run on every render. Should use individual setting values or a stable reference.
**Test coverage:** 7 E2E tests (cannot run in this env — missing system libs) / 5 API routes + 4 pages
**Doc accuracy:** Routes section incomplete (missing `/api/v1/commands`, `/api/v1/logs`). Rate limiting and `requireOwner` documented but not implemented.

---

### [02-smartadmin] SmartAdmin Dashboard (Express/EJS) — OBSOLETE
**Files read:** MDD doc only (all referenced source files deleted in Next.js migration)
**Findings:**
- LOW: This entire MDD doc is obsolete. All files it references (`views/`, `public/css/custom.css`, `public/js/dashboard.js`) have been deleted. Should be archived or removed.
**Test coverage:** N/A (feature replaced)
**Doc accuracy:** N/A (references deleted files)
